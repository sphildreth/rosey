# Fixtures

This folder stores stable test inputs for Rust tests and Python parity exports.

Suggested fixture groups:

```text
media_tree_001/
media_tree_sidecars/
media_tree_conflicts/
filenames/
nfo/
```

Avoid large binary media files. Use small placeholder files with realistic names.
